SELECT MAX(maxtemperaturef), zip
FROM weather
GROUP BY zip; --hottest day was 134 degrees in zip code 94063

SELECT start_station, COUNT(*)
FROM trips
GROUP BY start_station;

SELECT MIN(duration)
FROM trips;

SELECT end_station, AVG(duration)
FROM trips
GROUP BY end_station;
